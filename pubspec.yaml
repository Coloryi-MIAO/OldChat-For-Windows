name: Oldchat
description: Oldchat For Windows.
publish_to: 'none'
version: 1.3.8+9

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # 核心
  dio: ^5.4.0
  cryptography: ^2.9.0
  web_socket_channel: ^2.4.0
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  intl: ^0.18.0

  # UI 扩展
  photo_view: ^0.14.0
  image_picker: ^1.0.0
  file_picker: 8.0.5
  url_launcher: ^6.2.0
  package_info_plus: ^4.2.0
  webview_flutter: ^4.8.0
  webview_windows: ^0.4.0
  video_player: ^2.8.7
  chewie: ^1.7.5
  video_player_media_kit: ^1.0.0
  media_kit: 1.1.11
  media_kit_video: ^1.2.5
  media_kit_libs_windows_video: ^1.0.11
  win32: ^5.5.0
  ffi: ^2.1.3


  # 桌面功能
  window_manager: ^0.3.0
  tray_manager: ^0.5.2
  flutter_native_timezone: ^2.0.0
  path_provider: ^2.1.0

  # 音频（通知音）
  audioplayers: ^5.2.1
  flutter_local_notifications: ^17.0.0
  win_toast: ^0.4.0
  windows_single_instance: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
  fonts:
    - family: HarmonyOS Sans SC
      fonts:
        - asset: assets/HarmonyOS_Sans_SC_Regular.ttf
          weight: 400
        - asset: assets/HarmonyOS_Sans_SC_Medium.ttf
          weight: 500